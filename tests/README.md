# Host tests

Place desktop-hosted tests for hardware-independent modules here. Parser,
layout, and other pure logic should avoid Pico SDK headers so it can be built
and tested with the host compiler.

